# Command Logs

## GPIO Setup
sudo apt-get install python3-rpi.gpio

## MQTT Setup
pip install paho-mqtt gpiozero

## Telegram Setup
pip install telepot

## Config File Access
cat /etc/dhcpcd.conf

## Errors Observed
- "Invalid operation Rpi.GPIO"
- "vim: command not found"
